import { createContext } from 'react';

// const Context = createContext([0, () => {}]);

export const TemaContex = createContext(['Corinthians', 'Santos'])
export const NumeroContex = createContext(23)

export default TemaContex;